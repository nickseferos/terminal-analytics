<%= form_with(model: crane_operator, local: true) do |form| %>
  <% if crane_operator.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(crane_operator.errors.count, "error") %> prohibited this crane_operator from being saved:</h2>

      <ul>
      <% crane_operator.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :work_number %>
    <%= form.text_field :work_number %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
