<%= form_with(model: gang, local: true) do |form| %>
  <% if gang.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(gang.errors.count, "error") %> prohibited this gang from being saved:</h2>

      <ul>
      <% gang.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :flexed %>
    <%= form.check_box :flexed %>
  </div>

  <div class="field">
    <%= form.label :shift_id %>
    <%= form.text_field :shift_id %>
  </div>

  <div class="field">
    <%= form.label :top_pick %>
    <%= form.number_field :top_pick %>
  </div>

  <div class="field">
    <%= form.label :utr %>
    <%= form.number_field :utr %>
  </div>

  <div class="field">
    <%= form.label :note %>
    <%= form.text_area :note %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
